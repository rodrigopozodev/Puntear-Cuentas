import { Routes } from '@angular/router';
import { PunteoComponent } from './punteo/punteo.component';

export const routes: Routes = [
  { path: '', component: PunteoComponent }  // Ruta predeterminada
];
